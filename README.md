# 🌿 SmartGreenhouse — Кросплатформенний запуск через Vagrant

Цей репозиторій демонструє, як запускати застосунок **SmartGreenhouse** на різних операційних системах за допомогою **Vagrant**.

- **Windows** → запускається WPF-версія (**SmartGreenhouse**)
- **Linux (Ubuntu VM)** → запускається консольна версія (**SmartGreenhouse.Console**)
- Обидві версії використовують спільну бізнес-логіку .NET

---

## 🔧 Основна функціональність
- Підтримка кросплатформеної архітектури
- Єдина кодова база для UI та консольної логіки
- Автоматична підготовка Ubuntu-VM через provisioning
- Підтримка приватного NuGet-репозиторію (BaGet)
- Спільна папка `/vagrant` між хостом і VM

---

## 🚀 Як запустити

### 1. Клонування репозиторію
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>

### 2. Підняття віртуальної машини (Ubuntu)
vagrant up
vagrant ssh

Після входу у VM вже встановлені:
- .NET SDK
- .NET Runtime
- NuGet-конфіг (якщо BaGet використовується в проєкті)

### ▶️ Запуск застосунку

🟢 Linux (Ubuntu VM) — Console version
cd /vagrant/SmartGreenhouse.Console
dotnet run
# Запускає консольну симуляцію теплиці.

🟦 Windows — WPF UI
cd SmartGreenhouse
dotnet run
# Запускає повноцінний WPF-інтерфейс.

---

## 🛠 Примітки щодо Provisioning
Provisioning у Vagrantfile автоматично виконує:
- встановлення .NET SDK
- оновлення системи
- конфігурацію доступу до приватного NuGet-джерела (BaGet)
- можливе авто-збирання / авто-запуск (за потреби)

Папка `/vagrant` містить копію репозиторію із хоста.

---

## 📂 Структура репозиторію
SmartGreenhouse/            # WPF UI (Windows)
SmartGreenhouse.Console/    # Консольна версія (Linux/Windows)
SmartGreenhouse.Core/       # Спільна логіка (якщо винесено окремо)
Vagrantfile                 # Конфігурація VM
README.md                   # Цей файл

---

## 🌍 Підтримувані платформи
Платформа        Стан   Підтримка
Windows 10/11    ✔      WPF UI
Ubuntu (Vagrant VM) ✔   Console

---

## 🧪 Корисна інформація (NuGet / BaGet)

# 1. Як підняти VM:
vagrant up

# 2. Де доступний приватний NuGet-репозиторій:
http://192.168.56.10:5000

# 3. Як опублікувати NuGet-пакет:
dotnet nuget push <package>.nupkg --source "http://192.168.56.10:5000/v3/index.json"

# або PowerShell-скриптом:
./scripts/publish-package.ps1

# 4. Як встановити пакет у VM:
dotnet add package SmartGreenhouse --source baget