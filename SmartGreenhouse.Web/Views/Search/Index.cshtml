@model SmartGreenhouse.Web.Models.SearchViewModel

@{
    ViewData["Title"] = "Розширений пошук";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-primary text-white p-3">
                    <h3 class="mb-0"><i class="bi bi-search me-2"></i> Пошук вимірювань</h3>
                </div>
                <div class="card-body p-4 bg-light">
                    <form asp-action="Results" method="post">
                        
                        <div class="row g-3">
                            <!-- Вибір сенсора -->
                            <div class="col-md-12">
                                <label class="form-label fw-bold">📡 Оберіть сенсор</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-white"><i class="bi bi-hdd-network"></i></span>
                                    <select asp-for="SensorId" class="form-select">
                                        <option value="">— Шукати по всіх сенсорах —</option>
                                        @foreach (var s in Model.Sensors)
                                        {
                                            <option value="@s.Id">@s.Name (@(s.Plant?.Name ?? "Вільний"))</option>
                                        }
                                    </select>
                                </div>
                            </div>

                            <!-- Дати -->
                            <div class="col-md-6">
                                <label class="form-label fw-bold">📅 Дата від</label>
                                <input type="date" asp-for="FromDate" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold">📅 Дата до</label>
                                <input type="date" asp-for="ToDate" class="form-control" />
                            </div>

                            <div class="col-12"><hr class="text-muted"></div>

                            <!-- Температура -->
                            <div class="col-md-6">
                                <label class="form-label fw-bold">🌡️ Мін. Температура (°C)</label>
                                <input type="number" step="0.1" asp-for="MinValue" class="form-control" placeholder="напр. 18" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold">🌡️ Макс. Температура (°C)</label>
                                <input type="number" step="0.1" asp-for="MaxValue" class="form-control" placeholder="напр. 30" />
                            </div>
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-primary btn-lg shadow-sm">
                                <i class="bi bi-search"></i> Знайти записи
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>