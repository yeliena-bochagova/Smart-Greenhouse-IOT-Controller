@model IEnumerable<SmartGreenhouse.Web.Models.Measurement>

@{
    ViewData["Title"] = "Measurements";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>📊 Історія вимірювань</h2>
        <span class="text-muted">Останні записи</span>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0 align-middle text-center">
                <thead class="table-dark">
                    <tr>
                        <th class="text-start ps-4">📅 Дата та час</th>
                        <th class="text-start">📡 Сенсор / Рослина</th>
                        <th>Температура</th>
                        <th>Вологість</th>
                        <th>Освітлення</th>
                        <th class="text-end pe-4">Дії</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-start ps-4">
                                <!-- ТУТ МИ ВИПРАВЛЯЄМО ЧАС: .ToLocalTime() -->
                                <div class="fw-bold">@item.Timestamp.ToLocalTime().ToString("d")</div>
                                <small class="text-muted">@item.Timestamp.ToLocalTime().ToString("T")</small>
                            </td>
                            <td class="text-start">
                                @if (item.Sensor != null)
                                {
                                    <div class="fw-bold text-primary">@item.Sensor.Name</div>
                                    <small class="text-muted">
                                        <i class="bi bi-flower1"></i> @(item.Sensor.Plant?.Name ?? "Без рослини")
                                    </small>
                                }
                                else
                                {
                                    <span class="text-muted">Видалений сенсор</span>
                                }
                            </td>
                            <td>
                                <span class="badge rounded-pill bg-danger bg-opacity-10 text-danger border border-danger p-2">
                                    <i class="bi bi-thermometer-half"></i> @item.Temperature.ToString("F1") °C
                                </span>
                            </td>
                            <td>
                                <span class="badge rounded-pill bg-info bg-opacity-10 text-info border border-info p-2">
                                    <i class="bi bi-moisture"></i> @item.Humidity.ToString("F1") %
                                </span>
                            </td>
                            <td>
                                <span class="badge rounded-pill bg-warning bg-opacity-10 text-warning border border-warning p-2">
                                    <i class="bi bi-brightness-high"></i> @item.Light.ToString("F0") Lx
                                </span>
                            </td>
                            <td class="text-end pe-4">
                                <a href="/Measurements/Details/@item.Id" class="btn btn-sm btn-outline-secondary">
                                    Деталі
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>