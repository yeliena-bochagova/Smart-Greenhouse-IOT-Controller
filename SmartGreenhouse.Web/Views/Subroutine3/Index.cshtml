@model List<string>

@{
    ViewData["Title"] = "Історія подій";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>📋 Історія подій (Logs)</h2>
        <a asp-controller="Subroutine2" asp-action="Index" class="btn btn-secondary">⬅ Назад</a>
    </div>

    @if (Model == null || Model.Count == 0)
    {
        <div class="alert alert-warning">
            Логів поки що немає. Спробуйте щось увімкнути в теплиці!
        </div>
    }
    else
    {
        <div class="card shadow-sm">
            <ul class="list-group list-group-flush" style="max-height: 600px; overflow-y: auto;">
                @foreach (var log in Model)
                {
                    // Фарбуємо різні події в різні кольори для краси
                    string colorClass = "text-dark";
                    if (log.Contains("Error")) colorClass = "text-danger fw-bold";
                    else if (log.Contains("ON")) colorClass = "text-success";
                    else if (log.Contains("OFF")) colorClass = "text-muted";
                    else if (log.Contains("updated")) colorClass = "text-primary";

                    <li class="list-group-item @colorClass" style="font-family: monospace; font-size: 0.9rem;">
                        @log
                    </li>
                }
            </ul>
        </div>
    }
</div>