@model IEnumerable<SmartGreenhouse.Web.Models.Plant>

@{
    ViewData["Title"] = "Plants";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>🌿 Рослини в теплиці</h2>
        @* Якщо буде кнопка створення, її можна додати сюди *@
        @* <a asp-action="Create" class="btn btn-success">Додати рослину</a> *@
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-striped table-hover mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>Назва</th>
                        <th>Опис</th>
                        <th class="text-end">Дії</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="fw-bold">@item.Name</td>
                            <td>@item.Description</td>
                            <td class="text-end">
                                <a href="/Plants/Details/@item.Id" class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-eye"></i> Деталі
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>